package org.vstu.meaningtree.nodes.types.containers;

import org.vstu.meaningtree.nodes.Type;

public class UnorderedDictionaryType extends DictionaryType {
    public UnorderedDictionaryType(Type keyType, Type valueType) {
        super(keyType, valueType);
    }
}
